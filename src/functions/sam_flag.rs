////////////////////////////////////////////////////////////////////////////////////////////////////

/// Binary interpretation on a SAM flag struct.
pub trait SamFlag {
  fn interpret(
    &self,
    p: usize,
  ) -> bool;
}

////////////////////////////////////////////////////////////////////////////////////////////////////
